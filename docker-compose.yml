version: '3'
services:
  postgres:
    container_name: postgres
    image: postgres:18
    restart: unless-stopped

    ports:
      - '5432:5432'
    volumes:
      - ./data:/var/lib/postgresql
    env_file:
      - .env
    networks:
      - postgres
networks:
  postgres:
    driver: bridge
